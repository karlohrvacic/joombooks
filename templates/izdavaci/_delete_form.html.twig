<form method="post" action="{{ path('izdavaci_delete', {'id': izdavaci.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ izdavaci.id) }}">
    <button class="btn">Delete</button>
</form>
