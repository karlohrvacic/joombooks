{% extends 'base.html.twig' %}

{% block title %}Statusi{% endblock %}

{% block body %}
    <h1>Statusi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statusi.id }}</td>
            </tr>
            <tr>
                <th>Naziv</th>
                <td>{{ statusi.naziv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('statusi_index') }}">natrag na listu statusa</a>

    <a href="{{ path('statusi_edit', {'id': statusi.id}) }}">Uredi</a>

    {{ include('statusi/_delete_form.html.twig') }}
{% endblock %}
