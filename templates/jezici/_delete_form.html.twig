<form method="post" action="{{ path('jezici_delete', {'id': jezici.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jezici.id) }}">
    <button class="btn">Delete</button>
</form>
