{% extends 'base.html.twig' %}

{% block title %}Jezici{% endblock %}

{% block body %}
    <h1>Jezici</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jezici.id }}</td>
            </tr>
            <tr>
                <th>Ime</th>
                <td>{{ jezici.ime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jezici_index') }}">back to list</a>

    <a href="{{ path('jezici_edit', {'id': jezici.id}) }}">edit</a>

    {{ include('jezici/_delete_form.html.twig') }}
{% endblock %}
