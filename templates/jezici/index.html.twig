{% extends 'base.html.twig' %}

{% block title %}Jezici index{% endblock %}

{% block body %}
    <h1>Jezici index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ime</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for jezici in jezicis %}
            <tr>
                <td>{{ jezici.id }}</td>
                <td>{{ jezici.ime }}</td>
                <td>
                    <a href="{{ path('jezici_show', {'id': jezici.id}) }}">show</a>
                    <a href="{{ path('jezici_edit', {'id': jezici.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('jezici_new') }}">Create new</a>
{% endblock %}
