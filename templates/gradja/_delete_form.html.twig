<form method="post" action="{{ path('gradja_delete', {'id': gradja.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gradja.id) }}">
    <button class="btn">Delete</button>
</form>
