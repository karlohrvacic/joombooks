{% extends 'base.html.twig' %}

{% block title %}Prikaz posudbe{% endblock %}

{% block body %}
    <h1>Posudbe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ posudbe.id }}</td>
            </tr>
            <tr>
                <th>Id Gradje</th>
                <td>{{ posudbe.idGradje }}</td>
            </tr>
            <tr>
                <th>Broj Iskaznice Korisnika</th>
                <td>{{ posudbe.brojIskazniceKorisnika }}</td>
            </tr>
            <tr>
                <th>Datum Posudbe</th>
                <td>{{ posudbe.datumPosudbe ? posudbe.datumPosudbe|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datum Roka Vracanja</th>
                <td>{{ posudbe.datumRokaVracanja ? posudbe.datumRokaVracanja|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Datum Vracanja</th>
                <td>{{ posudbe.datumVracanja ? posudbe.datumVracanja|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('posudbe_index') }}">back to list</a>

    <a href="{{ path('posudbe_edit', {'id': posudbe.id}) }}">edit</a>

    {{ include('posudbe/_delete_form.html.twig') }}
{% endblock %}
