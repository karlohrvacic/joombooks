{% extends 'base.html.twig' %}

{% block title %}Izbornik{% endblock %}

{% block body %}
    <h1>Dobrodošli korisniče</h1>
    <p>{{ korisnik.ime }}</p>
    <p>{{ korisnik.prezime }}</p>
    <p>{{ korisnik.knjiznice.naziv }}</p>
    <p>{{ korisnik.knjiznice.adresa }}</p>
    <td><img width="100px" src="{{ korisnik.fotografija }}"></td>
    <a href="{{ path('pregled_knjiga') }}">Pretraži</a>
    <p>{{ korisnik.brojTrenutnoPosudenih }}</p>
    <a href="{{ path('posudjene_knjige_korisnika') }}">Posuđeno</a>
    <p>{{ korisnik.brojTrenutnoRezerviranih }}</p>
    <a href="{{ path('rezervirane_knjige_korisnika') }}">Rezervirano</a>
    <br>
    <a href="{{ path('korisnicki_profil') }}">profil</a>
    <a href="{{ path('korisnicke_obavijesti') }}">obavijesti</a>
    <a href="{{ path('radno_vrijeme') }}">radno vrijeme</a>
    <a href="{{ path('korisnicke_postavke') }}">postavke</a>
    <a href="https://stackoverflow.com/questions/tagged/php">info/pomoć</a>
    <a href="{{ path('app_logout') }}">odjava</a>
{% endblock %}
